#include<iostream>
using namespace std;
int main(){
    double a,b,mul;
    cout<<"Enter the number 1 and 2 for multiplication\n";
    cin>>a>>b;
    mul = a*b;
    cout<<" The product of "<<a<<" and "<<b<<" is "<<mul;
}